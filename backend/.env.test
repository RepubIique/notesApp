# Test Environment Configuration
PORT=3001
FRONTEND_URL=http://localhost:5173

# Authentication - will be set dynamically in tests
PASSWORD_A_HASH=test_hash_a
PASSWORD_B_HASH=test_hash_b
JWT_SECRET=test-secret-key-for-e2e-testing
HASH_SALT=10

# Supabase Configuration - using real values for integration tests
SUPABASE_URL=${SUPABASE_URL}
SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
